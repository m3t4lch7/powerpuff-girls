import React from 'react';

/**
 * Shows a simple page not found page.
 *
 * @returns {*}
 */
const PageNotFound = () => (
  <div className="PageNotFound">
    Sorry, page not found :(
  </div>
);

export default PageNotFound;
